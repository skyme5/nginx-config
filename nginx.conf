# Generated by nginxconfig.io
# https://nginxconfig.io/?0.domain=localhost&0.non_www=false&0.cdn&0.php=false&0.proxy&0.proxy_path=%2Fapi%2F&0.fallback_html&0.access_log_domain&0.error_log_domain&server_tokens&brotli&log_not_found
#pid /run/nginx.pid;
worker_processes 1;
worker_rlimit_nofile 2000;

events {
	multi_accept on;
	worker_connections 1000;
}

http {
	charset utf-8;
	sendfile on;
	tcp_nopush on;
	tcp_nodelay on;
	types_hash_max_size 4096;
	types_hash_bucket_size 128;
	client_max_body_size 16M;

	# MIME
	# include mime.types;
	include C:/nginx/conf/mime.types;
	default_type application/octet-stream;

	# logging
	open_log_file_cache max=1000 inactive=20s valid=1m min_uses=2;
	access_log logs/access.log;
	error_log logs/error.log;

	# SSL
	ssl_session_timeout 1d;
	ssl_session_cache shared:SSL:10m;
	ssl_session_tickets off;

	# Diffie-Hellman parameter for DHE ciphersuites
	ssl_dhparam E:/projects/personal/nginx-server/ssl/dhparam.pem;

	# Mozilla Intermediate configuration
	ssl_protocols TLSv1.2 TLSv1.3;
	ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

	# load configs
	# include /etc/nginx/conf.d/*.conf;
	include E:/projects/personal/nginx-server/nginxconfig.io/upstream.conf;
	include E:/projects/personal/nginx-server/sites-enabled/localhost.conf;
}
